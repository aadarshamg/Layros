SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production

# Write token for scripts/migrate-products.mjs only (project -> API -> Tokens
# -> Add API token, "Editor" permission). Not needed to run the Studio.
SANITY_API_TOKEN=
